const Title = ({ children }) => (
	<h4 className="
      text-lg bg-clip-text text-transparent 
      bg-gradient-to-r from-blue-300 via-purcple-300 to-red-400 pt-14 inline-block
      lg:text-2xl">
		{children}
	</h4>
);

export default Title;
